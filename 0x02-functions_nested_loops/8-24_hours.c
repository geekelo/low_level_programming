#include "main.h"
/**
 * jack_bauer - checks for letters only*
 *
 * Return: returns zero false and one and true
 *
 */

void jack_bauer(void)
{
int x, y, z, s;

for (x = '0'; x < '3'; x++)
{
	for (y = '0'; y <= 9; y++)
	{
		for (z = '0'; z < 6;  z++)
		{
			for (s = '0'; s <= 9; s++)
			{				
				_putchar('a');
				_putchar('\n')
			}
		}
	}
}
}