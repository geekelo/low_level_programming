#include "main.h"
#include <string.h>

/**
* _strlen - prints the length of the string
* @s: parameter
* Return: the length of the string
*/

int _strlen(char *s)
{
int v;

v = strlen(s);
return (v);
}
